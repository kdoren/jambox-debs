#!/usr/bin/make -f

export CONFIG=Release
export CFLAGS="-march=native"

%:
	dh $@

override_dh_auto_build:
	cd Builds/LinuxMakefile && make -j`nproc` Standalone

override_dh_auto_install:
	dh_install

override_dh_auto_clean:
	dh_clean
